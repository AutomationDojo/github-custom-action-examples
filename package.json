{
  "name": "pr-size-checker",
  "version": "1.1.0",
  "description": "GitHub Action to automatically label PRs based on size",
  "main": "index.js",
  "scripts": {
    "bundle": "npm run build",
    "build": "ncc build index.js -o dist --license licenses.txt",
    "package": "ncc build index.js -o dist --license licenses.txt",
    "all": "npm run build",
    "test": "echo \"No tests yet\"",
    "semantic-release": "semantic-release"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/automationDojo/github-custom-action-examples.git"
  },
  "keywords": [
    "github-actions",
    "pull-request",
    "pr-size",
    "automation"
  ],
  "author": "AutomationDojo",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "semantic-release": "^23.0.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1"
  }
}
